@model IEnumerable<Schaken.Data.Contestent>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TournementId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AspNetUsersId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EnlistTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TournementId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AspNetUsersId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EnlistTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
